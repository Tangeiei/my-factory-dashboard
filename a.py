import streamlit as st
import pandas as pd
import plotly.express as px
import os

# --- 1. ตั้งค่าหน้าจอ Dashboard ---
st.set_page_config(page_title="Factory Data Dashboard", layout="wide")

st.title("📊 ข้อมูลโรงงานประกอบกิจการจัดการสิ่งปฏิกูล")
st.markdown("---")

# ฟังก์ชันสำหรับโหลดข้อมูล (รองรับภาษาไทย)
def load_data():
    file_path = '1a.csv'
    if os.path.exists(file_path):
        try:
            # พยายามอ่านไฟล์ด้วย encoding แบบ utf-8-sig สำหรับภาษาไทย
            data = pd.read_csv(file_path, encoding='utf-8-sig')
            return data
        except:
            # ถ้ามีปัญหาเรื่องภาษา ให้ลองแบบมอก. 620
            return pd.read_csv(file_path, encoding='cp874')
    else:
        st.error("❌ ไม่พบไฟล์ 1a.csv ในระบบ")
        return pd.DataFrame()

df = load_data()

# ตรวจสอบว่ามีข้อมูลหรือไม่
if not df.empty:
    
    # --- 2. ส่วนของ Sidebar (เพิ่มข้อมูล & Filter) ---
    st.sidebar.header("⚙️ การจัดการข้อมูล")
    
    # ฟอร์มเพิ่มข้อมูล (ข้อ 2)
    with st.sidebar.expander("➕ เพิ่มข้อมูลโรงงานใหม่"):
        with st.form("add_form", clear_on_submit=True):
            f_name = st.text_input("ชื่อโรงงาน")
            f_prov = st.text_input("จังหวัด")
            f_capital = st.number_input("เงินทุนรวม (ล้านบาท)", min_value=0.0)
            f_type = st.selectbox("จำพวก", [1, 2, 3], index=2)
            submit = st.form_submit_button("บันทึกข้อมูลลงตาราง")
            
            if submit:
                new_row = {
                    'ชื่อโรงงาน': f_name,
                    'จังหวัด': f_prov,
                    'เงินทุนรวม (ล้านบาท)': f_capital,
                    'จำพวก': f_type
                }
                df = pd.concat([df, pd.DataFrame([new_row])], ignore_index=True)
                # บันทึกชั่วคราวบน Cloud (จะหายเมื่อรีเฟรชระบบใหญ่ แต่ช่วยให้เห็นผลทันที)
                df.to_csv('1a.csv', index=False, encoding='utf-8-sig')
                st.success("✅ เพิ่มข้อมูลเรียบร้อย!")

    # ตัวกรองข้อมูล (ข้อ 5)
    st.sidebar.markdown("---")
    st.sidebar.subheader("🔍 ตัวกรอง")
    prov_list = sorted(df['จังหวัด'].dropna().unique().tolist())
    selected_prov = st.sidebar.multiselect("เลือกจังหวัด:", options=prov_list, default=prov_list[:5])

    # กรองข้อมูล
    filtered_df = df[df['จังหวัด'].isin(selected_prov)]

    # --- 3. การแสดงผลสถิติ (ข้อ 4) ---
    col1, col2 = st.columns(2)

    with col1:
        st.subheader("🏭 จำนวนโรงงานแยกตามจังหวัด")
        count_df = filtered_df['จังหวัด'].value_counts().reset_index()
        count_df.columns = ['จังหวัด', 'จำนวน']
        
        # กราฟแท่งแสดงตัวเลขบนหัวแท่ง
        fig_bar = px.bar(count_df, x='จังหวัด', y='จำนวน', text='จำนวน', color='จังหวัด')
        fig_bar.update_traces(textposition='outside')
        st.plotly_chart(fig_bar, use_container_width=True)

    with col2:
        st.subheader("💰 สัดส่วนเงินทุนรวมแยกตามจังหวัด")
        fig_pie = px.pie(filtered_df, values='เงินทุนรวม (ล้านบาท)', names='จังหวัด')
        st.plotly_chart(fig_pie, use_container_width=True)

    # --- 4. แสดงรายละเอียดทั้งหมด (ข้อ 6) ---
    st.markdown("---")
    st.subheader("📋 รายละเอียดข้อมูลทั้งหมด")
    st.dataframe(filtered_df, use_container_width=True)

    # ลูกโป่งฉลองเมื่อรันสำเร็จ
    st.balloons()

else:
    st.warning("⚠️ ไม่พบข้อมูลที่จะแสดงผล กรุณาตรวจสอบไฟล์ 1a.csv")
